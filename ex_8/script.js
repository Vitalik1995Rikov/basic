if (browser === 'Edge') {
  alert ('You have got the Edge')
} else if (browser === 'Chrome' || 'Firefox' || 'Safari') {
  alert ('We support this browser too')
} else {
  alert ('We hope that this page looks ok')
}